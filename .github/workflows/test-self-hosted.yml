name: Test Self-Hosted Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: [self-hosted, windows, docker]  # ← Make sure this matches your runner's labels!

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run commands
        run: |
          echo "Test from self-hosted runner win-docker-01"
          dir
          whoami
          Get-Date
          docker --version
