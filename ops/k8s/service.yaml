apiVersion: v1
kind: Service
metadata:
  name: trading-bot
  labels:
    app: trading-bot
spec:
  type: ClusterIP
  selector:
    app: trading-bot
  ports:
  - name: http
    port: 80
    targetPort: 8000
