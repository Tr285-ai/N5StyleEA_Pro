apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: trading-bot
  labels:
    app: trading-bot
spec:
  selector:
    matchLabels:
      app: trading-bot
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
